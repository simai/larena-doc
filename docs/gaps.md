# Known documentation and product gaps

Observed on 2026-09-14.

| Gap | Effect on documentation | Resolution owner |
| --- | --- | --- |
| The twelve-package slice does not expose a documented generic command for creating an arbitrary content model and admin screen | The tutorial verifies the existing bounded collection path and stops before inventing an API | Larena product/package owners |
| `larena/lang` package repository reports a pre-codegen baseline while integrated fallback behavior is tested in Root | The package page separates repository maturity from integrated evidence | `larena/lang` owner |
| Several packages expose developer foundations rather than stable public APIs | Reference pages name verified entry points and avoid compatibility guarantees | package owners |
| Full browser upload/delete is not accepted for the filesystem slice | File operations are described as guarded backend/integration behavior | Filesystem/Admin owners |
| Six declared dataview types are not six accepted production UI implementations | The page documents normalized descriptors and the verified table/query path only | Dataview/UI owners |
| Public production readiness is outside the current release-candidate evidence | All pages label the target as a local developer foundation | Root/release owners |
| English localization is not authored yet | Stable `/ru/` routes and translation keys preserve a later paired locale path | documentation owner |
